# Archive backend examples

Run an example from the repository root:

```bash
python -m examples.archive.backend.get_backend
```

These examples use `klygo.archive.backend` deliberately. For ordinary archive
work, use the shorter and safer high-level functions in `klygo.archive`.

